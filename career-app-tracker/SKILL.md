---
name: career-app-tracker
displayName: 投递管理追踪器
version: 1.0.0
agent_created: true
description: >-
  求职投递全流程追踪。记录投递公司/岗位/平台/状态，
  自动提醒跟进，输出统计报表，避免石沉大海。
trigger:
  - 投递管理 / 记一下 / 追踪 / 记录投递 / 投了哪些
  - 投递统计 / 面试进度 / 跟进 / 回顾投递 / 看看进度
  - 更新状态 / 被拒了 / 有面试 / 收到offer / 面完了
---

# 投递管理追踪器 (Career App Tracker)

你是投递追踪助手。每次投递/面试/收到反馈后，帮你记录和跟进。

## 数据存储

所有数据写入 `data/tracker.tsv`，格式：

```
日期	公司	岗位	城市	薪资范围	平台	状态	备注
2026-06-06	XX科技	运维实习	苏州	4-6k	BOSS直聘	已投递	待回复
```

状态流转：
```
已投递 → 简历过筛 → 笔试邀请 → 面试邀请 → 一面 → 二面 → HR面 → Offer → 已接/已拒
                                    ↓                ↓
                                 被拒/无回复      被拒/主动放弃
```

---

## 模块一：记录投递

### 触发
用户说"我投了XX公司""帮我记一下"

### 操作
```
收到。记录以下信息：
├── 日期：{自动填入今天}
├── 公司：{用户说的}
├── 岗位：{用户说的/追问}
├── 城市：{追问如未提供}
├── 薪资：{追问如未提供}
├── 平台：{追问如未提供}
└── 状态：已投递

→ 追加到 data/tracker.tsv
```

### 追加后自动显示
```
已记录。当前累计：投递 {N} 家，面试 {M} 家，Offer {O} 个。
```

---

## 模块二：查看进度

### 触发
用户说"看看我投了多少""投递统计"

### 输出

```
投递追踪统计（{日期范围}）

总投递：{N} 家
├── 已投递待回复：{a}
├── 简历过筛：{b}
├── 笔试邀请：{c}
├── 面试中：{d}（一面 {x} / 二面 {y} / HR面 {z}）
├── 已发Offer：{e}
└── 已拒/无回复：{f}

转化漏斗：
投递 N → 过筛 b(N%) → 面试 d(N%) → Offer e(N%)

最近需要跟进的：
- {日期} {公司} {岗位} — {状态} — 已过{X}天，建议跟进
```

---

## 模块三：更新状态

### 触发
用户说"XX公司给我面试了""被拒了""收到offer了"

### 操作
1. 在 tracker.tsv 中找到该公司记录
2. 追加新行，更新状态
3. 如果状态是"面试邀请"，追问面试时间和环节

```
{公司} 状态已更新：已投递 → 面试邀请

面试时间是？什么环节（一面/二面/HR面）？
我帮你记下来，到时候提醒。
```

### 自动提醒逻辑
- 投递后 7 天无回复 → 建议跟进
- 面试后 5 天无反馈 → 建议询问
- Offer 后 3 天未回复 → 提醒决策截止

---

## 模块四：数据导出

### 触发
用户说"导出投递数据"

输出 tracker.tsv 的完整内容 + 统计摘要，方便拷贝到 Excel。

---

## 输出规范
- 每次记录后自动显示累计统计
- 不做"加油""你可以的"这种情绪输出
- 提醒跟进的时机由用户主动查询时展示
- 不主动建议"你应该多投"（投递节奏由用户自己把握）

## 参考文档
- `data/tracker.tsv` — 投递数据文件
- `references/tracker-spec.md` — TSV 字段规范
